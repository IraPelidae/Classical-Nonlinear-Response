#pragma once

const double N = 4050; //No_rods
const double T = 1; //temperature
const double L = 30; //length of the system
const double rod_length = 0.001;
const int no_samples = 200; //number of simulation runs (i.e. sample size)